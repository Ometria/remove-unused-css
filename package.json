{
  "name": "remove-unused-css",
  "version": "1.0.0",
  "description": "Small module for filtering out unused css rules.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require babel-register tests/*",
    "postinstall": "babel -s -d build --presets es2015 index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "cheerio": "^0.22.0",
    "css": "^2.2.1"
  },
  "devDependencies": {
    "babel-register": "^6.24.1",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
